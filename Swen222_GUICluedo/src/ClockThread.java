import gameOfCluedo.GameOfCluedo;

public class ClockThread extends Thread {
	/*private final int delay; // delay between pulses in us
	private final GameOfCluedo goc;
	private final CluedoFrame frame;*/

	/*public ClockThread(int delay, Board game, BoardFrame display) {
		this.delay = delay;
		this.game = game;
		this.display = display;
	}

	public void run() {
		while(1 == 1) {
			// Loop forever
			try {
				Thread.sleep(delay);
				game.clockTick();
				if(display != null) {
					display.repaint();
				}
			} catch(InterruptedException e) {
				// should never happen
			}
		}
	}*/
}
